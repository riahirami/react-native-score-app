import Navigation from './navigation/NavigationContainer';
import React from 'react';

/**
 * Represents the whole app layout and the parent of element's hierarchy
 * @returns JSX.Element
 */
const Layout = (): JSX.Element => {
  return <Navigation />;
};

export default Layout;
