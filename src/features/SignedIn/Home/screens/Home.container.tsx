import React from 'react';

import Home from './Home';

/**
 * Container used to seperate home logic as a wrapper to Home screen
 * @returns JSX.Element
 */
const HomeContainer = (): JSX.Element => {
  return <Home />;
};

export default HomeContainer;
