import React from 'react';
import { View } from 'react-native';

/**
 * Represents Home screen ui
 * @returns JSX.Element
 */
const Home = (): JSX.Element => {
  return <View />;
};

export default Home;
