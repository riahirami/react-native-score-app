import React from 'react';
import {View} from 'react-native';

/**
 * Represents Login screen ui
 * @returns JSX.Element
 */
const Login = (): JSX.Element => {
  return <View />;
};

export default Login;
