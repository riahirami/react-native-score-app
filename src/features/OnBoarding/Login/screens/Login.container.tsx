import React from 'react';

import Login from './Login';

/**
 * Container used to seperate login logic as a wrapper to Login screen
 * @returns JSX.Element
 */
const LoginContainer = (): JSX.Element => {
  return <Login />;
};

export default LoginContainer;
