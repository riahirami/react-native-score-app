import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  playerNameContainer: {
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    alignItems: 'center',
    gap: 16,
    marginHorizontal: 8,
    paddingVertical: 16
  }
});
