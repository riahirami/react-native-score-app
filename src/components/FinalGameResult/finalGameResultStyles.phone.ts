import { colors } from '_utils/colors';
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  imageDineriContainer: {
    bottom: 0,
    left: 0
  },
  imageStyle: {
    width: 100,
    height: 120
  },
  winText: { color: colors.PRIMARY }
});
